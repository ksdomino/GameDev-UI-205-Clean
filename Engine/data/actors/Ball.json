{
  "_context": "Engine/llms.txt",
  "_description": "Ball actor - bounces between paddles",
  "id": "Ball",
  "type": "projectile",
  "description": "The ball that bounces between paddles. Scores when it exits the play area.",
  "layer": "SPRITES",
  "sprite": {
    "shape": "circle",
    "radius": 25,
    "color": "#ffffff",
    "assetId": null
  },
  "variables": {
    "positionX": { "type": "number", "default": 540, "description": "Current X position (center of screen)" },
    "positionY": { "type": "number", "default": 960, "description": "Current Y position (center of screen)" },
    "directionX": { "type": "number", "default": 1, "description": "Horizontal direction (-1 left, 1 right)" },
    "directionY": { "type": "number", "default": -1, "description": "Vertical direction (-1 up, 1 down)" },
    "speed": { "type": "number", "default": 600, "description": "Movement speed in pixels per second" },
    "baseSpeed": { "type": "number", "default": 600, "description": "Starting speed (for reset)" },
    "speedIncrement": { "type": "number", "default": 25, "description": "Speed increase per paddle hit" },
    "maxSpeed": { "type": "number", "default": 1200, "description": "Maximum ball speed" }
  },
  "tags": ["ball", "projectile"],
  "collidesWith": ["PlayerPaddle", "AIPaddle"],
  "behaviors": ["move", "bounce", "score"]
}
